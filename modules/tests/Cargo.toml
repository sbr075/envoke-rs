[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dependencies]
envoke = { path = "../envoke" }
serde = { version = "1.0.218", features = ["derive"] }
strum = { version = "0.27.1", features = ["derive"] }
temp-env = "0.3.6"
